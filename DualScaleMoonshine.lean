-- Stream 4 root: Mathieu moonshine computed, not typed (see docs/STREAM4_WORKFLOW.md).
-- Separate `lean_lib`; imports Stream 2 (for `eotA`) and, through it, Stream 1 (for `M24RepDim`).

import DualScaleMoonshine.QSeries
import DualScaleMoonshine.Twining
import DualScaleMoonshine.Characters
import DualScaleMoonshine.ForgerTest
import DualScaleMoonshine.TwiningAll
import DualScaleMoonshine.CharactersAll
import DualScaleMoonshine.Shadow
